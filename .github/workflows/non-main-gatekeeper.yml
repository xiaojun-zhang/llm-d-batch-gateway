name: Non-Main Gatekeeper
on:
  workflow_dispatch:  # Manual trigger only - disabled for feature branch collaboration
  # pull_request:
  #   types: [opened, edited, synchronize, reopened]

permissions:
  pull-requests: write

jobs:
  gatekeeper:
    uses: llm-d/llm-d-infra/.github/workflows/reusable-non-main-gatekeeper.yaml@6c2d2e60e7fee83e2a300688f6d4d7a2339c2d74 # main
