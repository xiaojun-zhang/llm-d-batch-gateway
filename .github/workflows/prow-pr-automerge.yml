name: Prow Auto-merge
on:
  workflow_dispatch:  # Manual trigger only - automerge disabled for manual control
  # schedule:
  #   - cron: "*/15 * * * *"

permissions:
  contents: write
  pull-requests: write

jobs:
  auto-merge:
    uses: llm-d/llm-d-infra/.github/workflows/reusable-prow-automerge.yaml@6c2d2e60e7fee83e2a300688f6d4d7a2339c2d74 # main
